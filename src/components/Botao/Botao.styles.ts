// Botao.styles.ts
import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    botao: {
        backgroundColor: '#21486F',
        width: '95%',
        height: 55,
        borderRadius: 7,
        justifyContent: 'center',
        alignItems: 'center',
    },
    textoBotao: {
        color: '#757C86',
        textAlign: 'center',
        fontSize: 20,
    },
});

export default styles;
